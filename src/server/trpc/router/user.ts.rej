diff a/src/server/trpc/router/user.ts b/src/server/trpc/router/user.ts	(rejected hunks)
@@ -1,7 +1,6 @@
-import { inferQueryOutput } from "../../pages/_app"
-
-import { createRouter } from "../context"
-import { prisma } from "../db/client"
+import { RouterOutputs } from "../../../utils/trpc"
+import { createRouter } from "../../context"
+import { prisma } from "../../db/client"
 import { z } from "zod"
 
 export const userRouter = createRouter().query("getByProjectId", {
@@ -30,4 +29,15 @@ export const userRouter = createRouter().query("getByProjectId", {
   },
 })
 
-export type UserGetByProjectIdOutput = inferQueryOutput<"user.getByProjectId">
+export type UserBreakdownStory = {
+  id: string
+  title: string
+  estimate: number
+  investedEffort: number
+  remainingEffort: number
+  totalEffort: number
+  type: string
+  state: string
+}
+
+export type UserGetByProjectIdOutput = RouterOutputs["user"]["getByProjectId"]
